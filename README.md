# 진로맵 Career Compass Pro

고등학생과 학부모가 대학·학과 선택을 직업, 기업, 연봉 수준, 생활 수준, 근무강도, 장단점, 리스크, 미래전망, 고등학교 준비 로드맵까지 연결해 보는 전문 진로 설계 웹앱입니다.

## 운영 원칙

- 모바일과 PC에서 모두 볼 수 있는 웹앱으로 만든다.
- 사용자의 MacBook이 꺼져 있어도 접속 가능해야 한다.
- 정적 배포는 GitHub Pages를 기본값으로 한다.
- 최신성이 중요한 입시·채용·연봉 정보는 앱이 단정하지 않고 공식 자료와 검색 링크로 확인하게 한다.

## 실행 방법

```bash
pnpm install
pnpm dev
```

정적 배포 빌드:

```bash
pnpm build
```

GitHub Pages 배포는 `.github/workflows/pages.yml`이 `main` 브랜치 push 때 자동으로 `out/` 결과물을 배포합니다.

## 주요 기능

- 홈 대시보드
- CareerFit 360 진로진단: 빠른/표준/심층 모드
- 학과맵 및 학과 상세
- 대학맵 및 대학 상세
- 직업맵, 직업 상세, 세부 직무 상세
- 기업맵 및 기업 상세
- 비교실
- 메디컬 특화관
- 반도체 특화관
- 나의 진로 포트폴리오
- 학생·학부모·상담교사용 리포트
- 관리자 미리보기

## 데이터 검증 원칙

- 대학과 학과 연결은 공식 데이터로 확인되기 전까지 `검증 필요`로 표시한다.
- 임의 생성된 대학-학과 조합은 실제 설치 학과처럼 표현하지 않는다.
- 모든 대학·학과·직무 데이터는 출처, 기준연도, 검증상태를 가질 수 있도록 설계한다.
- 최종 입시는 각 대학 입학처, 대입정보포털 어디가, 대학알리미를 통해 확인한다.
- 연봉과 채용현황은 단정하지 않고 범위, 변동성, 공식 검색 링크로 안내한다.

## 데이터 구조

핵심 타입은 `src/types/career.ts`에 분리했습니다.

- `Major`: 학과, 과목, 성향, 관련 대학·직업·기업, 연봉, 생활수준, 장단점, 리스크
- `Job`: 직업, 진입경로, 자격/면허, 연봉, 생활수준, 미래전망
- `Company`: 기업, 산업, 채용 링크, OpenDART 검색, 관련 직무
- `University`, `UniversityMajor`: 대학과 모집단위 연결
- `Department`, `MajorCluster`: 전국 대학·학과 데이터 확장을 위한 구조
- `CareerField`, `Occupation`, `JobRole`: 직업을 실제 직무 단위까지 확장하는 구조
- `ComparisonPreset`: 비교실 프리셋

초기 데이터는 `src/data/mockData.ts`에 있습니다.

공식 데이터 연동을 위한 폴더 구조:

- `src/data/seed/`: 앱에 바로 쓰는 핵심 시드 데이터
- `src/data/generated/`: 공공데이터/API 연동 결과가 들어갈 JSON 자리
- `src/data/source/academyinfo/`: 대학알리미 원자료
- `src/data/source/careernet/`: 커리어넷 원자료
- `src/data/source/worknet/`: 워크넷/고용24 원자료
- `src/data/source/ncs/`: NCS 직무분류 원자료
- `src/scripts/`: 가져오기, 정규화, 검증, 검색 인덱스 생성 스크립트 자리

## 향후 API 연동 계획

- 대학알리미: 학과, 취업률, 등록금, 장학금
- 대입정보포털 어디가: 모집단위, 전형, 입시 정보
- 커리어넷: 직업·학과 정보
- 워크넷/고용24: 채용정보
- OpenDART: 기업 공시
- YouTube Data API: 직무 인터뷰와 학과 소개 영상 큐레이션
- Supabase: 사용자 계정, 진단 결과, 포트폴리오 저장

## 연봉 데이터 주의사항

연봉 정보는 참고용 추정치입니다. 실제 연봉은 기업, 지역, 경력, 성과급, 근무형태, 개원·창업 여부에 따라 크게 달라질 수 있습니다. 평균 하나보다 경력별 범위, 소득 변동성, 워라밸, 건강, 가족시간, 번아웃 위험을 함께 봐야 합니다.

## 입시 데이터 주의사항

이 앱은 진로 탐색과 의사결정을 돕는 참고 도구입니다. 최종 입시 정보는 반드시 각 대학 입학처와 공식 모집요강을 확인해야 합니다. 모집단위명, 전형, 정원, 계약학과 조건, 장학·취업연계 조건은 매년 바뀔 수 있습니다.

## 미성년자 개인정보 보호

현재 버전은 브라우저 localStorage에만 포트폴리오를 저장합니다. 실서비스에서 사용자 계정을 도입할 경우 학생 개인정보, 상담 기록, 성향 진단 결과는 최소 수집, 보호자 동의, 접근권한 분리, 삭제 요청 절차를 포함해야 합니다.

## 고도화 로드맵

Phase 1: mock data 기반 MVP, 학과/직업/기업 상세, 비교실, 진단 결과, 포트폴리오

Phase 2: CareerFit 360 진단, 전체 전공계열 확장, 세부 직무 그래프, 대학 상세, 검증 상태 표시

Phase 3: 공공데이터 API, 대학알리미, 어디가, 커리어넷, 워크넷/고용24, OpenDART, YouTube Data API 연동

Phase 4: Supabase 연동, 사용자 계정, 진단 결과 저장, 학부모/학생 계정 연결, 상담교사 모드

Phase 5: PDF 리포트 다운로드, 학생별 진로 변화 추적, 학년별 준비 알림, AI 기반 탐구보고서 주제 추천, 대학별 입시 캘린더

Phase 6: 실제 연봉 데이터 업데이트, 채용시장 트렌드 분석, 직무별 성장성 분석, 진로 시뮬레이션, 10년 뒤 삶의 모습 시나리오 기능
